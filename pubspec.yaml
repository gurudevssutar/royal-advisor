name: drag_drop_listview_example
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  rive: ^0.7.22
  cached_network_image: ^3.1.0
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # drag & drop listview
  drag_and_drop_lists: ^0.3.1
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/